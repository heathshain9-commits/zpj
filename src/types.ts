export interface ChapterDetail {
  label: string;
  title: string;
  desc: string;
  image: string;
  tone: string;
  element: string;
  color: string;
}

export interface InspirationBubble {
  id: number;
  title: string;
  icon: string;
  desc: string;
  top: string;
  left: string;
}
